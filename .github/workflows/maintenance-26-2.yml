name: Retired Paper 26.2 repair

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired
        run: echo "The one-time Paper 26.2 repair workflow is retired. Changes must be committed directly and verified by the main build workflow."